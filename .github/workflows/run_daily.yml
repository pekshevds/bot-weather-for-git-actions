name: daily git actions weather bot
on: 
    push:
jobs:
    Daily-Run:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
            - name: Run App
              run: python app.py